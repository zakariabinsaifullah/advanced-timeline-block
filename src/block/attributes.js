const attributes= {
    // timeline Content
    timebarColor: {
        type: 'string',
        default: '#000000',
    },
    timelineStyle: {
        type: 'string',
        default: 'both_sides',
    }
};

export default attributes; 
